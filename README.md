Clone this repo and execute command:

`mvn spring-boot:run`

to run this app.

In order to have a UI for this app you need to clone and run frontend part (https://github.com/Lanretipmes/egar-task-front).
